#pragma once
#include "Pch.h"

#ifndef MATH_DELTATIME
#define MATH_DELTATIME() (1.0f / 60.0f)
#endif

namespace ACSU_Math
{
    // 前方宣言
    struct Vector2;
    struct Vector2Int;
    struct Vector3;
    struct Vector3Int;
    struct Vector4;
    struct Quaternion;
    struct Matrix4x4;
    struct Plane;
    struct Rect;
    struct Ray;
    struct Bounds;
    struct Color;
}