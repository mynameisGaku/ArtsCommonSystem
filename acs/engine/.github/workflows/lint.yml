# SPDX-License-Identifier: Apache-2.0
# ACS C++ lint gate。C++ sourceを変更するpushとPRでformatとtidyを検証する。
name: lint

on:
  push:
    branches: [main, dev]
    paths:
      - "src/**"
      - "tests/**"
      - ".clang-format"
      - ".clang-tidy"
      - "CMakeLists.txt"
      - "cmake/**"
      - ".github/workflows/lint.yml"
  pull_request:
    branches: [main, dev]
    paths:
      - "src/**"
      - "tests/**"
      - ".clang-format"
      - ".clang-tidy"
      - "CMakeLists.txt"
      - "cmake/**"

jobs:
  format:
    name: clang-format dry-run
    runs-on: windows-2022
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: clang-format --dry-run
        shell: pwsh
        run: |
          # PR changed files only; on push to main, all files
          if ($env:GITHUB_EVENT_NAME -eq 'pull_request') {
              $files = git diff --name-only --diff-filter=AM `
                  origin/$($env:GITHUB_BASE_REF)...HEAD `
                  -- 'src/*.h' 'src/*.hpp' 'src/*.cpp' 'src/*.inl' `
                     'tests/*.h' 'tests/*.cpp'
          } else {
              $files = git ls-files `
                  'src/*.h' 'src/*.hpp' 'src/*.cpp' 'src/*.inl' `
                  'tests/*.h' 'tests/*.cpp'
          }
          if (-not $files) { exit 0 }
          clang-format --dry-run --Werror @files

  tidy:
    name: clang-tidy (acs_lint)
    runs-on: windows-2022
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          submodules: recursive

      - uses: lukka/get-cmake@latest

      - name: Configure
        shell: pwsh
        run: |
          cmake -S . -B build -G "Ninja" `
                -DACS_BUILD_TESTS=ON `
                -DACS_RENDER_DX12_RAW=ON `
                -DCMAKE_BUILD_TYPE=Debug `
                -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Run acs_lint target
        shell: pwsh
        run: cmake --build build --target acs_lint
