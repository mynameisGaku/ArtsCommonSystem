{
  "schema": 2,
  "title": "Result を返す関数で、戻り値を確認せずに使ったら止まった",
  "item": {
    "q": "Result を返す関数で、戻り値を確認せずに使ったら止まった",
    "tags": [
      "Result",
      "エラー処理"
    ],
    "a": "IsOk() で確認する前に Value() を呼ぶと、失敗時にそこで(アサートで)停止します。 必ず if (r.IsOk()) を通してから Value()、失敗時は r.Error() を見て分岐してください。"
  }
}
