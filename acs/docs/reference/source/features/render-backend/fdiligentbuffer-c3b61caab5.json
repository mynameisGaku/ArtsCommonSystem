{
  "schema": 2,
  "module": {
    "id": "render-backend",
    "title": "render — 描画バックエンド(DX12 / Diligent)",
    "description": "GPU に実際に絵を描かせるRHI(描画ハードウェア抽象)の中身。 共通インターフェイス IRhiDevice 等を、Windows ネイティブの DX12 と、複数 API を束ねる Diligent の 2 系統で実装します。 ふだんはこれらを直接 new せず、CreateRhiDevice() 等のファクトリ越しに IRhi* ハンドルだけ触ります。 ここに載っているのは「どのバックエンドが裏で動いているか」を知りたい・拡張したい人向けの内部実装です。"
  },
  "feature": {
    "name": "FDiligentBuffer",
    "kind": "クラス",
    "header": "render/Diligent/DiligentBuffer.h",
    "summary": "Diligent の GPU バッファ(IRhiBuffer 派生)。 Diligent の IBuffer を頂点・インデックス・定数バッファとして確保し、FDx12Buffer と同じ API で更新できます。",
    "when": "CreateRhiBuffer() が Diligent で生成する実体。",
    "sample": "FBufferDesc d;\nd.size = sizeof(CB); d.usage = EBufferUsage::Uniform;\nd.cpu_writable = true;\nauto cb = CreateRhiBuffer(*dev, d).Value();\ncb->Update(&data, sizeof(data));",
    "members": [
      {
        "sig": "usize Size() const",
        "desc": "バイト数。"
      },
      {
        "sig": "EBufferUsage Usage() const",
        "desc": "用途。"
      },
      {
        "sig": "void Update(const void* data, usize size, usize offset=0)",
        "desc": "CPU から書き込む(cpu_writable=true のみ)。"
      },
      {
        "sig": "Diligent::IBuffer* Native() const",
        "ret": "生バッファ",
        "desc": "内部公開。 生の IBuffer。"
      }
    ]
  }
}
