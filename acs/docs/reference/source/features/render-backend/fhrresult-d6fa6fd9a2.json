{
  "schema": 2,
  "module": {
    "id": "render-backend",
    "title": "render — 描画バックエンド(DX12 / Diligent)",
    "description": "GPU に実際に絵を描かせるRHI(描画ハードウェア抽象)の中身。 共通インターフェイス IRhiDevice 等を、Windows ネイティブの DX12 と、複数 API を束ねる Diligent の 2 系統で実装します。 ふだんはこれらを直接 new せず、CreateRhiDevice() 等のファクトリ越しに IRhi* ハンドルだけ触ります。 ここに載っているのは「どのバックエンドが裏で動いているか」を知りたい・拡張したい人向けの内部実装です。"
  },
  "feature": {
    "name": "FHrResult",
    "kind": "構造体",
    "header": "render/Dx12/Dx12Common.h",
    "summary": "DX12 内部用の小さな結果ラッパ。 HRESULT(Win32 のエラーコード)を保持し、ACS の Result 系と橋渡しします。",
    "when": "DX12 バックエンドの Init 系が返す内部型。 利用側コードでは普通 TResult を見るので、これを直接触ることは稀。",
    "sample": "FHrResult r = device.Init(cfg);\nif (r.IsErr()) { /* HRESULT は r.hr */ }",
    "members": [
      {
        "sig": "HRESULT hr",
        "desc": "生の HRESULT(既定 S_OK)。"
      },
      {
        "sig": "bool IsOk() const / bool IsErr() const",
        "desc": "成功(SUCCEEDED) / 失敗(FAILED)判定。"
      }
    ]
  }
}
