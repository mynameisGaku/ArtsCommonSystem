/* ACS リファレンス — ガイド / トラブルシュート / 用語集シード（手書き）。
   ここは品質の手本。各モジュール data/<module>.js も同じ <t>用語</t> 記法・口調で書く。 */

// ============================ はじめに & 規約 ============================
ACS_REF.guide = [
  { h2: "ACS とは" },
  { p: "ACS は日本のインディー開発者向けの軽量な C++ <t>ゲームフレームワーク</t>です。Windows / <t>DirectX 12</t> 上で動き、<t>STL</t>(標準ライブラリ)を使わず、独自の小さな部品で構成されています。難しい所は隠しつつ、必要なら中身まで触れる、を狙っています。" },
  { note: "とにかく早く動かしたい人は <b>acs::easy</b>(簡単モード)から。<code>OpenWindow</code> → <code>NextFrame</code> ループで絵が出ます。慣れてきたら下の層に降りていけます。" },

  { h2: "3 つの名前空間" },
  { p: "コードは大きく 3 つの<t>名前空間</t>に分かれます。どの層を使っているかを意識すると迷いません。" },
  { ul: [
    "<b>acs</b> … 基盤(型・コンテナ・メモリ・数学・描画・ECS など)。エンジン本体。",
    "<b>acs::easy</b> … 初心者向けの簡単モード。関数を呼ぶだけでウィンドウ・描画・入力・ジョブが使える薄い層。",
    "<b>acs::game</b> … ゲーム作りの部品(シーン・コンポーネント・各ジャンルキット)。GameFramework モジュール。"
  ]},

  { h2: "名前の付け方(接頭辞)" },
  { p: "型名の頭文字でだいたいの種類が分かります。" },
  { ul: [
    "<b>T…</b> … <t>テンプレート</t>(型を後から指定する部品)。例: <code>TArray&lt;int&gt;</code>, <code>TSharedPtr&lt;Mesh&gt;</code>",
    "<b>F…</b> … ふつうのクラス/構造体。例: <code>FVec3</code>, <code>FThreadPool</code>",
    "<b>E…</b> … 列挙(<t>enum</t>、選択肢の集合)。例: <code>EKey</code>, <code>EMemoryOrder</code>",
    "<b>I…</b> … <t>インターフェース</t>(実装を差し替えられる約束事)。例: <code>IAssetLoader</code>",
    "<b>m_…</b> … メンバ変数(クラスが内部に持つ変数)。"
  ]},

  { h2: "エラーは Result で返す(例外を使わない)" },
  { p: "ACS は<t>例外</t>(throw/try-catch)を使いません。失敗し得る関数は <code>Result&lt;T, E&gt;</code> を返します。成功なら値(T)、失敗なら理由(E)が入っていて、呼ぶ側が必ず確認します。" },
  { code: "Result<FFile, EIoError> r = OpenFile(\"a.txt\");\nif (r.IsOk()) {\n    FFile f = r.Unwrap();   // 成功: 中身を取り出す\n} else {\n    EIoError e = r.Err();   // 失敗: 理由\n}" },
  { note: "<code>Ok(value)</code> / <code>Err(code)</code> で作り、<code>IsOk()</code> で確認、<code>Unwrap()</code> で取り出します。確認せず Unwrap すると失敗時に止まります。", kind: "warn" },

  { h2: "STL を使わない理由と代わり" },
  { p: "ビルド時間・バイナリサイズ・挙動の予測しやすさのため、<code>std::vector</code> などの代わりに ACS 独自のコンテナを使います。覚え方は『std の名前を T/F に置き換える』です。" },
  { ul: [
    "<code>std::vector</code> → <code>TArray</code>",
    "<code>std::unordered_map</code> → <code>THashMap</code>",
    "<code>std::string</code> → <code>FString</code>",
    "<code>std::unique_ptr</code> → <code>TUniquePtr</code>",
    "<code>std::shared_ptr</code> → <code>TSharedPtr</code>(旧 TRc)",
    "<code>std::weak_ptr</code> → <code>TWeakPtr</code>"
  ]},

  { h2: "所有権とメモリ" },
  { p: "誰がそのメモリを解放するのか(<t>所有権</t>)をはっきりさせるのが安全の基本です。生 <t>ポインタ</t>で <code>new</code>/<code>delete</code> を手書きするより、<t>スマートポインタ</t>に任せます。詳しくは memory モジュールへ。" },
  { ul: [
    "1 人だけが持つ → <code>TUniquePtr</code>",
    "みんなで共有する → <code>TSharedPtr</code>",
    "生死だけ見たい(持たない) → <code>TWeakPtr</code> / <code>TWeakObjectPtr</code>"
  ]},

  { h2: "2D は Y 軸が下向き" },
  { p: "ACS の 2D 座標は<b>左上が原点 (0,0)、下に行くほど Y が増える</b>(Y-down)規約です。数学の授業と上下が逆なので注意。画面の縦サイズが H なら、一番下は y=H 付近です。" },

  { h2: "ビルドと実行" },
  { p: "Visual Studio 用の <code>ACS.slnx</code> を <code>acs/generate.ps1</code> で生成し、<code>acs/Intermediate/vs</code> にプロジェクトが出ます。実行ファイルは <code>acs/Binaries/&lt;構成&gt;</code>(Debug/Release) に出ます。" },
  { note: "生成スクリプトは <t>BOM</t> 付き UTF-8 必須。MSBuild のターゲット名は snake_case(例 <code>hello_jobs</code>)です。", kind: "warn" }
];

// ============================ トラブルシュート ============================
ACS_REF.troubleshooting = [
  { q: "ビルドはできたのに、ウィンドウが真っ黒/何も描画されない", tags:["描画","真っ黒","window"],
    a: "<t>描画</t>ループ(<code>while(NextFrame())</code>)の中で毎フレーム描画関数を呼べているか確認。easy 層なら <code>NextFrame()</code> が裏で画面クリア→提示をしています。座標が画面外(特に 2D は <b>Y-down</b>なので下方向)になっていないかも要確認。<br><b>必ず実機で目視確認すること。</b> 過去に『プロセスは生きているのに何も描いていない』事故があり、起動成功(alive)だけでは不十分です。" },
  { q: "ジョブ(ParallelFor / RunAsync)の中で描画したら落ちた/おかしくなった", tags:["ジョブ","スレッド","クラッシュ"],
    a: "ジョブのラムダは<b>別<t>スレッド</t>で動きます</b>。描画など easy/レンダラの関数はメインスレッド専用なので、ジョブの中では<b>計算だけ</b>して結果を変数に書き、描画は <code>while</code> ループ側(メインスレッド)で行ってください。" },
  { q: "TSharedPtr 同士が互いを持ち合って、いつまでもメモリが解放されない", tags:["メモリ","リーク","スマートポインタ","循環参照"],
    a: "<t>循環参照</t>です。A が B を <code>TSharedPtr</code> で持ち、B も A を <code>TSharedPtr</code> で持つと<t>参照カウント</t>が 0 にならず永遠に残ります。片方を <code>TWeakPtr</code>(弱参照)にして輪を断ち切ってください。親→子は強参照、子→親は弱参照、が定石です。" },
  { q: "TWeakPtr.Lock() がいつも空(無効)になる", tags:["スマートポインタ","TWeakPtr"],
    a: "対象がすでに破棄されています(強参照が 0 になった)。<code>TWeakPtr</code> は生存を延ばしません。使う直前に <code>Lock()</code> し、戻り値が有効な間だけ触ってください。生成直後に Lock が空なら、元の <code>TSharedPtr</code> が先に消えていないか確認。" },
  { q: "TObjectPtr / TWeakObjectPtr を生ポインタから作ったら空になる", tags:["スマートポインタ","FObject"],
    a: "その対象が <code>NewObject&lt;T&gt;()</code> 経由で作られていないと、内部の制御ブロックが無く空になります。<code>FObject</code> 派生は <code>NewObject</code> で生成してください(スタックや素の <code>new</code> ではダメ)。" },
  { q: "Result を返す関数で、戻り値を確認せずに使ったら止まった", tags:["Result","エラー処理"],
    a: "<code>IsOk()</code> で確認する前に <code>Unwrap()</code> すると、失敗時にそこで停止します。必ず <code>if (r.IsOk())</code> を通してから <code>Unwrap()</code>、失敗時は <code>r.Err()</code> を見て分岐してください。" },
  { q: "std::vector や std::string を使ったらビルドが通らない/方針に合わない", tags:["STL","コンテナ"],
    a: "ACS は <t>STL</t> を使わない方針です。<code>TArray</code> / <code>FString</code> / <code>THashMap</code> など独自コンテナに置き換えてください(『はじめに』の対応表参照)。" },
  { q: "generate.ps1 を実行してもプロジェクトが更新されない/文字化けする", tags:["ビルド","generate","BOM"],
    a: "生成スクリプトや一部入力は <b>UTF-8 (BOM 付き)</b>が前提です。エディタの保存エンコーディングを確認。新しいサンプルを足したら <code>cmake -S engine -B Intermediate/vs</code> で再構成が必要です。" },
  { q: "新しく追加したサンプルがビルド対象に出てこない", tags:["ビルド","サンプル","cmake"],
    a: "サンプルは <code>engine/CMakeLists.txt</code> に <code>acs_add_sample(NN_Name)</code> で明示登録します。ウィンドウを開くサンプルは <code>ACS_RENDER_DX12_RAW</code> ガード内に入れます。登録後に CMake 再構成してください。" },
  { q: "リンクエラー(未解決のシンボル)が出る", tags:["ビルド","リンク","LNK"],
    a: "そのモジュールを <code>target_link_libraries(... PRIVATE ACS::Xxx)</code> でリンクしているか、ヘッダだけで実体(.cpp)をビルドに含め忘れていないか確認。モジュールは <code>src/&lt;mod&gt;/Module.cmake</code> の SOURCES/HEADERS に列挙します。" },
  { q: "実行ファイルが見つからない / どこに出る？", tags:["ビルド","実行","exe"],
    a: "<code>acs/Binaries/&lt;構成&gt;</code>(例 <code>acs/Binaries/Debug</code>)に出ます。MSBuild ターゲット名は snake_case(例 <code>hello_jobs</code>)。リポジトリは OneDrive 外の作業コピーで扱う運用です。" },
  { q: "ウィンドウ版サンプルが『たまにカクつく』", tags:["パフォーマンス","描画"],
    a: "まず <b>Release</b> で実行(Debug は重く・ブレやすい)。毎フレームの <code>ParallelFor</code> は粒度(grain)を大きめにしてタスク投入数を減らすと安定します。極端に軽い処理を並列化するとオーバーヘッドが勝つので、その場合は直列でも可。" },
  { q: "毎フレーム ParallelFor しているのに速くならない", tags:["ジョブ","パフォーマンス"],
    a: "1 回の処理が軽すぎるとタスク投入・待ち合わせのコストが勝ちます。要素数を増やすか、1 要素あたりの計算を増やすか、grain を大きくして直列寄りにしてください。並列化が効くのは『十分に重い処理』です。" },
  { q: "DirectX 12 デバイスの作成に失敗する/起動直後に落ちる", tags:["描画","DX12","クラッシュ"],
    a: "GPU ドライバが古い、または DX12 非対応環境の可能性。ドライバ更新を試し、別の DX12 サンプルが動くか確認。デバッグレイヤのメッセージ(出力ウィンドウ)に原因が出ることが多いです。" },
  { q: "2D のスプライトが思った場所と上下逆/ずれる", tags:["2D","座標","Y-down"],
    a: "ACS の 2D は <b>Y-down</b>(左上原点・下が +Y)。数学座標のつもりで上下を反転して計算していないか確認。画面下端は y≈画面高さ です。" },
  { q: "アロケータを差し替えたい / フレーム毎に大量確保して遅い", tags:["メモリ","アロケータ","パフォーマンス"],
    a: "用途別に使い分けます。毎フレーム捨てる一時確保は <code>FArenaAllocator</code>(リセットで一括解放)、汎用は <code>TSharedPtr</code>/<code>MakeShared</code> の既定(<code>DefaultAllocator</code>)。<code>MakeSharedIn(alloc, ...)</code> でアロケータを明示指定できます。" },
  { q: "マルチスレッドでカウンタや変数を更新したら値が壊れる", tags:["スレッド","アトミック","競合"],
    a: "複数スレッドが同じ変数を同時に書くと<t>データ競合</t>になります。カウンタ類は <code>TAtomic&lt;T&gt;</code> を使うか、<code>FMutex</code> で囲ってください。読むだけ/各自別の要素を書くだけなら競合しません。" },
  { q: "音を鳴らしたいが鳴らない", tags:["音","audio"],
    a: "オーディオエンジンの初期化と、音アセットの読み込み成否(<code>Result</code>)を確認。サンプルによってはバックエンドのコードはあるが未接続で実発音まで未確認のものがあります(チュートリアル注記参照)。" },
  { q: "デバッグだと動くが Release で挙動が変わる", tags:["ビルド","最適化"],
    a: "未初期化変数・<t>未定義動作</t>(配列外・寿命切れポインタ等)が Release の最適化で表面化した可能性。スマートポインタで寿命を明確にし、配列アクセス範囲を見直してください。" },
  { q: "ヘッダを増やしたらコンパイルが急に遅くなった", tags:["ビルド","コンパイル時間"],
    a: "重いヘッダの広域 include が原因のことが多いです。前方宣言で済む所は .h で include せず .cpp に回す、テンプレートの肥大化を避ける、を検討してください。" },
  { q: "『どの層の関数を使えばいいか』が分からない", tags:["設計","easy"],
    a: "まず <b>acs::easy</b> で作って動かし、足りなくなった機能だけ下層(acs / acs::game)に降りるのがおすすめ。easy は薄いラッパなので、混在させても問題ありません。" }
];

// ============================ 用語集シード ============================
Object.assign(ACS_REF.glossary, {
  "ゲームフレームワーク": "ゲーム作りに必要な土台(描画・入力・音・シーン管理など)をまとめて提供する部品集。エンジンより薄く、自由度が高い。",
  "DirectX 12": "Windows の低レベル<t>描画</t>API。GPU を細かく制御できる代わりに手間も多い。ACS が裏で使う。",
  "描画": "画面に絵(図形・画像・文字)を出すこと。レンダリングとも言う。",
  "STL": "C++ 標準ライブラリ(std::vector など)。ACS はビルド時間や予測しやすさのため使わず独自コンテナを用いる。",
  "名前空間": "名前の衝突を防ぐ入れ物。<code>acs::easy::OpenWindow</code> の <code>acs::easy</code> の部分。",
  "テンプレート": "型を後から指定できる部品。<code>TArray&lt;int&gt;</code> は『int の配列』というように中身の型を差し替えられる。",
  "enum": "決まった選択肢の集合を表す型(列挙)。例: 上下左右のキー。",
  "インターフェース": "実装を差し替えられるようにするための約束事(純粋仮想クラス)。I で始まる。",
  "例外": "throw/try-catch で行うエラー処理の仕組み。ACS では使わず <t>Result</t> で返す。",
  "Result": "成功なら値、失敗なら理由を入れて返す型 <code>Result&lt;T,E&gt;</code>。<t>例外</t>の代わり。",
  "所有権": "そのメモリ/資源を『誰が解放する責任を持つか』。はっきりさせると安全。",
  "ポインタ": "メモリ上の場所(アドレス)を指す値。中身そのものではなく『在りか』を持つ。",
  "スマートポインタ": "確保したメモリを自動で解放してくれるポインタ風オブジェクト。手で delete しなくてよい。<t>RAII</t> の代表例。",
  "参照カウント": "そのオブジェクトを今いくつの所有者が指しているかの数。0 になったら破棄する。<t>TSharedPtr</t> が使う仕組み。",
  "弱参照": "対象を所有せず(寿命を延ばさず)、生きているかだけを見られる参照。<t>循環参照</t>を断ち切るのに使う。",
  "循環参照": "AがBを、BがAを<t>参照カウント</t>付きで持ち合い、互いに 0 にならず解放されない状態。片方を<t>弱参照</t>にして解消する。",
  "RAII": "オブジェクトの寿命に資源(メモリ・ファイル等)の確保/解放を結びつける手法。スコープを抜けると自動で片付く。",
  "ヒープ": "実行中に好きなサイズを確保できるメモリ領域。確保/解放のコストがある。",
  "スタック": "関数の呼び出しに沿って自動で確保/解放される高速なメモリ。寿命はスコープに縛られる。",
  "アロケータ": "メモリの確保/解放のやり方を担当する部品。用途に応じて差し替えられる。",
  "スレッド": "同時に動く処理の流れ。複数あると並列に計算できるが、同じデータを同時に触ると壊れ得る。",
  "アトミック": "複数スレッドから同時に触っても壊れない不可分な操作/変数。<code>TAtomic</code>。",
  "データ競合": "複数スレッドが同じ変数を同時に読み書きして結果が壊れること。<t>アトミック</t>や排他で防ぐ。",
  "ミューテックス": "一度に1つのスレッドだけが通れる鍵。共有データを守る。<code>FMutex</code>。",
  "コールバック": "あとで呼んでもらうために渡す関数。イベント発生時などに実行される。",
  "ラムダ": "その場で書く名前のない小さな関数。<code>[&](int i){ ... }</code> の形。",
  "ハンドル": "中身を直接持たず、表から参照するための小さな識別子(番号など)。",
  "デストラクタ": "オブジェクトが破棄される時に自動で呼ばれる後始末の関数(~ClassName)。",
  "ムーブ": "中身をコピーせず所有権ごと移すこと。大きなデータを安く引き渡せる。",
  "未定義動作": "C++ 規格が結果を保証しない操作(配列外アクセス・寿命切れポインタ等)。最適化で表面化しやすい。",
  "BOM": "テキスト先頭に付く文字コードの目印。ACS の一部スクリプトは UTF-8 BOM 付きが前提。",
  "ホバー": "マウスを乗せること。乗せると説明が出る、の意。",
  "GPU": "画像処理に特化した演算装置。描画はここで行われる。"
});
