/* ACS リファレンス — ガイド / トラブルシュート / 用語集シード（手書き）。
   ここは品質の手本。各モジュール data/<module>.js も同じ <t>用語</t> 記法・口調で書く。 */

// ============================ はじめに & 規約 ============================
ACS_REF.guide = [
  { h2: "ACS とは" },
  { p: "ACS は日本のインディー開発者向けの軽量な C++ <t>ゲームフレームワーク</t>です。Windows / <t>DirectX 12</t> 上で動き、<t>STL</t>(標準ライブラリ)を使わず、独自の小さな部品で構成されています。難しい所は隠しつつ、必要なら中身まで触れる、を狙っています。" },
  { note: "とにかく早く動かしたい人は <b>acs::easy</b>(簡単モード)から。<code>OpenWindow</code> → <code>NextFrame</code> ループで絵が出ます。慣れてきたら下の層に降りていけます。" },

  { h2: "3 つの名前空間" },
  { p: "コードは大きく 3 つの<t>名前空間</t>に分かれます。どの層を使っているかを意識すると迷いません。" },
  { ul: [
    "<b>acs</b> … 基盤(型・コンテナ・メモリ・数学・描画・ECS など)。エンジン本体。",
    "<b>acs::easy</b> … 初心者向けの簡単モード。関数を呼ぶだけでウィンドウ・描画・入力・ジョブが使える薄い層。",
    "<b>acs::game</b> … ゲーム作りの部品(シーン・コンポーネント・各ジャンルキット)。GameFramework モジュール。"
  ]},

  { h2: "名前の付け方(接頭辞)" },
  { p: "型名の頭文字でだいたいの種類が分かります。" },
  { ul: [
    "<b>T…</b> … <t>テンプレート</t>(型を後から指定する部品)。例: <code>TArray&lt;int&gt;</code>, <code>TSharedPtr&lt;Mesh&gt;</code>",
    "<b>F…</b> … ふつうのクラス/構造体/union。例: <code>FVec3</code>, <code>FThreadPool</code>",
    "<b>A…</b> … <code>FObject</code> の強参照/弱参照所有モデルで扱う node/component。例: <code>ANode</code>, <code>AComponent</code>",
    "<b>E…</b> … 列挙(<t>enum</t>、選択肢の集合)。例: <code>EKey</code>, <code>EMemoryOrder</code>",
    "<b>I…</b> … <t>インターフェース</t>(実装を差し替えられる約束事)。例: <code>IAssetLoader</code>",
    "<b>型 alias</b> … 頭文字は指定しません。delegate / callback も意味の明確な PascalCase なら <code>HotReloadCallback</code> と <code>FHotReloadCallback</code> のどちらも使えます。新規宣言は <code>using</code> を使い、<code>typedef</code> はlegacy互換に限ります。",
    "<b>m_…</b> … メンバ変数(クラスが内部に持つ変数)。"
  ]},

  { h2: "エラーは Result で返す(例外を使わない)" },
  { p: "ACS は<t>例外</t>(throw/try-catch)を使いません。失敗し得る関数は <code>TResult&lt;T, E&gt;</code>(E 既定は <code>FErrorCode</code>)を返します。成功なら値(T)、失敗なら理由(E)が入っていて、呼ぶ側が必ず確認します。" },
  { code: "TResult<FFile, EIoError> r = OpenFile(\"a.txt\");\nif (r.IsOk()) {\n    FFile f = r.Value();    // 成功: 中身を取り出す\n} else {\n    EIoError e = r.Error(); // 失敗: 理由\n}" },
  { note: "<code>Ok(value)</code> / <code>Err(code)</code> で作り、<code>IsOk()</code> で確認、<code>Value()</code> で取り出します。確認せず <code>Value()</code> を呼ぶと失敗時に<t>アサート</t>で止まります。", kind: "warn" },

  { h2: "STL を使わない理由と代わり" },
  { p: "ビルド時間・バイナリサイズ・挙動の予測しやすさのため、<code>std::vector</code> などの代わりに ACS 独自のコンテナを使います。覚え方は『std の名前を T/F に置き換える』です。" },
  { ul: [
    "<code>std::vector</code> → <code>TArray</code>",
    "<code>std::unordered_map</code> → <code>THashMap</code>",
    "<code>std::string</code> → <code>FString</code>",
    "<code>std::unique_ptr</code> → <code>TUniquePtr</code>",
    "<code>std::shared_ptr</code> → <code>TSharedPtr</code>(旧 TRc)",
    "<code>std::weak_ptr</code> → <code>TWeakPtr</code>"
  ]},

  { h2: "所有権とメモリ" },
  { p: "誰がそのメモリを解放するのか(<t>所有権</t>)をはっきりさせるのが安全の基本です。生 <t>ポインタ</t>で <code>new</code>/<code>delete</code> を手書きするより、<t>スマートポインタ</t>に任せます。詳しくは memory モジュールへ。" },
  { ul: [
    "1 人だけが持つ → <code>TUniquePtr</code>",
    "みんなで共有する → <code>TSharedPtr</code>",
    "生死だけ見たい(持たない) → <code>TWeakPtr</code> / <code>TWeakObjectPtr</code>"
  ]},

  { h2: "2D は Y 軸が下向き" },
  { p: "ACS の 2D 座標は<b>左上が原点 (0,0)、下に行くほど Y が増える</b>(Y-down)規約です。数学の授業と上下が逆なので注意。画面の縦サイズが H なら、一番下は y=H 付近です。" },

  { h2: "ビルドと実行" },
  { p: "Visual Studio 用の <code>ACS.slnx</code> を <code>acs/generate.ps1</code> で生成し、<code>acs/Intermediate/vs</code> にプロジェクトが出ます。実行ファイルは <code>acs/Binaries/&lt;構成&gt;</code>(Debug/Release) に出ます。" },
  { note: "生成スクリプトは <t>BOM</t> 付き UTF-8 必須。MSBuild のターゲット名は snake_case(例 <code>hello_jobs</code>)です。", kind: "warn" }
];

// ============================ トラブルシュート ============================
ACS_REF.troubleshooting = [
  { q: "ビルドはできたのに、ウィンドウが真っ黒/何も描画されない", tags:["描画","真っ黒","window"],
    a: "<t>描画</t>ループ(<code>while(NextFrame())</code>)の中で毎フレーム描画関数を呼べているか確認。easy 層なら <code>NextFrame()</code> が裏で画面クリア→提示をしています。座標が画面外(特に 2D は <b>Y-down</b>なので下方向)になっていないかも要確認。<br><b>必ず実機で目視確認すること。</b> 過去に『プロセスは生きているのに何も描いていない』事故があり、起動成功(alive)だけでは不十分です。" },
  { q: "ジョブ(ParallelFor / RunAsync)の中で描画したら落ちた/おかしくなった", tags:["ジョブ","スレッド","クラッシュ"],
    a: "ジョブのラムダは<b>別<t>スレッド</t>で動きます</b>。描画など easy/レンダラの関数はメインスレッド専用なので、ジョブの中では<b>計算だけ</b>して結果を変数に書き、描画は <code>while</code> ループ側(メインスレッド)で行ってください。" },
  { q: "TSharedPtr 同士が互いを持ち合って、いつまでもメモリが解放されない", tags:["メモリ","リーク","スマートポインタ","循環参照"],
    a: "<t>循環参照</t>です。A が B を <code>TSharedPtr</code> で持ち、B も A を <code>TSharedPtr</code> で持つと<t>参照カウント</t>が 0 にならず永遠に残ります。片方を <code>TWeakPtr</code>(弱参照)にして輪を断ち切ってください。親→子は強参照、子→親は弱参照、が定石です。" },
  { q: "TWeakPtr.Lock() がいつも空(無効)になる", tags:["スマートポインタ","TWeakPtr"],
    a: "対象がすでに破棄されています(強参照が 0 になった)。<code>TWeakPtr</code> は生存を延ばしません。使う直前に <code>Lock()</code> し、戻り値が有効な間だけ触ってください。生成直後に Lock が空なら、元の <code>TSharedPtr</code> が先に消えていないか確認。" },
  { q: "TObjectPtr / TWeakObjectPtr を生ポインタから作ったら空になる", tags:["スマートポインタ","FObject"],
    a: "その対象が <code>NewObject&lt;T&gt;()</code> 経由で作られていないと、内部の制御ブロックが無く空になります。<code>FObject</code> 派生は <code>NewObject</code> で生成してください(スタックや素の <code>new</code> ではダメ)。" },
  { q: "Result を返す関数で、戻り値を確認せずに使ったら止まった", tags:["Result","エラー処理"],
    a: "<code>IsOk()</code> で確認する前に <code>Value()</code> を呼ぶと、失敗時にそこで(<t>アサート</t>で)停止します。必ず <code>if (r.IsOk())</code> を通してから <code>Value()</code>、失敗時は <code>r.Error()</code> を見て分岐してください。" },
  { q: "std::vector や std::string を使ったらビルドが通らない/方針に合わない", tags:["STL","コンテナ"],
    a: "ACS は <t>STL</t> を使わない方針です。<code>TArray</code> / <code>FString</code> / <code>THashMap</code> など独自コンテナに置き換えてください(『はじめに』の対応表参照)。" },
  { q: "generate.ps1 を実行してもプロジェクトが更新されない/文字化けする", tags:["ビルド","generate","BOM"],
    a: "生成スクリプトや一部入力は <b>UTF-8 (BOM 付き)</b>が前提です。エディタの保存エンコーディングを確認。新しいサンプルを足したら <code>cmake -S engine -B Intermediate/vs</code> で再構成が必要です。" },
  { q: "新しく追加したサンプルがビルド対象に出てこない", tags:["ビルド","サンプル","cmake"],
    a: "サンプルは <code>engine/CMakeLists.txt</code> に <code>acs_add_sample(NN_Name)</code> で明示登録します。ウィンドウを開くサンプルは <code>ACS_RENDER_DX12_RAW</code> ガード内に入れます。登録後に CMake 再構成してください。" },
  { q: "リンクエラー(未解決のシンボル)が出る", tags:["ビルド","リンク","LNK"],
    a: "そのモジュールを <code>target_link_libraries(... PRIVATE ACS::Xxx)</code> でリンクしているか、ヘッダだけで実体(.cpp)をビルドに含め忘れていないか確認。モジュールは <code>src/&lt;mod&gt;/Module.cmake</code> の SOURCES/HEADERS に列挙します。" },
  { q: "実行ファイルが見つからない / どこに出る？", tags:["ビルド","実行","exe"],
    a: "<code>acs/Binaries/&lt;構成&gt;</code>(例 <code>acs/Binaries/Debug</code>)に出ます。MSBuild ターゲット名は snake_case(例 <code>hello_jobs</code>)。リポジトリは OneDrive 外の作業コピーで扱う運用です。" },
  { q: "ウィンドウ版サンプルが『たまにカクつく』", tags:["パフォーマンス","描画"],
    a: "まず <b>Release</b> で実行(Debug は重く・ブレやすい)。毎フレームの <code>ParallelFor</code> は粒度(grain)を大きめにしてタスク投入数を減らすと安定します。極端に軽い処理を並列化するとオーバーヘッドが勝つので、その場合は直列でも可。" },
  { q: "毎フレーム ParallelFor しているのに速くならない", tags:["ジョブ","パフォーマンス"],
    a: "1 回の処理が軽すぎるとタスク投入・待ち合わせのコストが勝ちます。要素数を増やすか、1 要素あたりの計算を増やすか、grain を大きくして直列寄りにしてください。並列化が効くのは『十分に重い処理』です。" },
  { q: "DirectX 12 デバイスの作成に失敗する/起動直後に落ちる", tags:["描画","DX12","クラッシュ"],
    a: "GPU ドライバが古い、または DX12 非対応環境の可能性。ドライバ更新を試し、別の DX12 サンプルが動くか確認。デバッグレイヤのメッセージ(出力ウィンドウ)に原因が出ることが多いです。" },
  { q: "2D のスプライトが思った場所と上下逆/ずれる", tags:["2D","座標","Y-down"],
    a: "ACS の 2D は <b>Y-down</b>(左上原点・下が +Y)。数学座標のつもりで上下を反転して計算していないか確認。画面下端は y≈画面高さ です。" },
  { q: "アロケータを差し替えたい / フレーム毎に大量確保して遅い", tags:["メモリ","アロケータ","パフォーマンス"],
    a: "用途別に使い分けます。毎フレーム捨てる一時確保は <code>FArenaAllocator</code>(リセットで一括解放)、汎用は <code>TSharedPtr</code>/<code>MakeShared</code> の既定(<code>DefaultAllocator</code>)。<code>MakeSharedIn(alloc, ...)</code> でアロケータを明示指定できます。" },
  { q: "マルチスレッドでカウンタや変数を更新したら値が壊れる", tags:["スレッド","アトミック","競合"],
    a: "複数スレッドが同じ変数を同時に書くと<t>データ競合</t>になります。カウンタ類は <code>TAtomic&lt;T&gt;</code> を使うか、<code>FMutex</code> で囲ってください。読むだけ/各自別の要素を書くだけなら競合しません。" },
  { q: "音を鳴らしたいが鳴らない", tags:["音","audio"],
    a: "オーディオエンジンの初期化と、音アセットの読み込み成否(<code>Result</code>)を確認。サンプルによってはバックエンドのコードはあるが未接続で実発音まで未確認のものがあります(チュートリアル注記参照)。" },
  { q: "デバッグだと動くが Release で挙動が変わる", tags:["ビルド","最適化"],
    a: "未初期化変数・<t>未定義動作</t>(配列外・寿命切れポインタ等)が Release の最適化で表面化した可能性。スマートポインタで寿命を明確にし、配列アクセス範囲を見直してください。" },
  { q: "ヘッダを増やしたらコンパイルが急に遅くなった", tags:["ビルド","コンパイル時間"],
    a: "重いヘッダの広域 include が原因のことが多いです。前方宣言で済む所は .h で include せず .cpp に回す、テンプレートの肥大化を避ける、を検討してください。" },
  { q: "『どの層の関数を使えばいいか』が分からない", tags:["設計","easy"],
    a: "まず <b>acs::easy</b> で作って動かし、足りなくなった機能だけ下層(acs / acs::game)に降りるのがおすすめ。easy は薄いラッパなので、混在させても問題ありません。" }
];

// ============================ 用語集シード ============================
Object.assign(ACS_REF.glossary, {
  "ゲームフレームワーク": "ゲーム作りに必要な土台(描画・入力・音・シーン管理など)をまとめて提供する部品集。エンジンより薄く、自由度が高い。",
  "DirectX 12": "Windows の低レベル<t>描画</t>API。GPU を細かく制御できる代わりに手間も多い。ACS が裏で使う。",
  "描画": "画面に絵(図形・画像・文字)を出すこと。レンダリングとも言う。",
  "STL": "C++ 標準ライブラリ(std::vector など)。ACS はビルド時間や予測しやすさのため使わず独自コンテナを用いる。",
  "名前空間": "名前の衝突を防ぐ入れ物。<code>acs::easy::OpenWindow</code> の <code>acs::easy</code> の部分。",
  "テンプレート": "型を後から指定できる部品。<code>TArray&lt;int&gt;</code> は『int の配列』というように中身の型を差し替えられる。",
  "enum": "決まった選択肢の集合を表す型(列挙)。例: 上下左右のキー。",
  "インターフェース": "実装を差し替えられるようにするための約束事(純粋仮想クラス)。I で始まる。",
  "例外": "throw/try-catch で行うエラー処理の仕組み。ACS では使わず <t>Result</t> で返す。",
  "Result": "成功なら値、失敗なら理由を入れて返す型 <code>Result&lt;T,E&gt;</code>。<t>例外</t>の代わり。",
  "所有権": "そのメモリ/資源を『誰が解放する責任を持つか』。はっきりさせると安全。",
  "ポインタ": "メモリ上の場所(アドレス)を指す値。中身そのものではなく『在りか』を持つ。",
  "スマートポインタ": "確保したメモリを自動で解放してくれるポインタ風オブジェクト。手で delete しなくてよい。<t>RAII</t> の代表例。",
  "参照カウント": "そのオブジェクトを今いくつの所有者が指しているかの数。0 になったら破棄する。<t>TSharedPtr</t> が使う仕組み。",
  "弱参照": "対象を所有せず(寿命を延ばさず)、生きているかだけを見られる参照。<t>循環参照</t>を断ち切るのに使う。",
  "循環参照": "AがBを、BがAを<t>参照カウント</t>付きで持ち合い、互いに 0 にならず解放されない状態。片方を<t>弱参照</t>にして解消する。",
  "RAII": "オブジェクトの寿命に資源(メモリ・ファイル等)の確保/解放を結びつける手法。スコープを抜けると自動で片付く。",
  "ヒープ": "実行中に好きなサイズを確保できるメモリ領域。確保/解放のコストがある。",
  "スタック": "関数の呼び出しに沿って自動で確保/解放される高速なメモリ。寿命はスコープに縛られる。",
  "アロケータ": "メモリの確保/解放のやり方を担当する部品。用途に応じて差し替えられる。",
  "スレッド": "同時に動く処理の流れ。複数あると並列に計算できるが、同じデータを同時に触ると壊れ得る。",
  "アトミック": "複数スレッドから同時に触っても壊れない不可分な操作/変数。<code>TAtomic</code>。",
  "データ競合": "複数スレッドが同じ変数を同時に読み書きして結果が壊れること。<t>アトミック</t>や排他で防ぐ。",
  "ミューテックス": "一度に1つのスレッドだけが通れる鍵。共有データを守る。<code>FMutex</code>。",
  "コールバック": "あとで呼んでもらうために渡す関数。イベント発生時などに実行される。",
  "ラムダ": "その場で書く名前のない小さな関数。<code>[&](int i){ ... }</code> の形。",
  "ハンドル": "中身を直接持たず、表から参照するための小さな識別子(番号など)。",
  "デストラクタ": "オブジェクトが破棄される時に自動で呼ばれる後始末の関数(~ClassName)。",
  "ムーブ": "中身をコピーせず所有権ごと移すこと。大きなデータを安く引き渡せる。",
  "未定義動作": "C++ 規格が結果を保証しない操作(配列外アクセス・寿命切れポインタ等)。最適化で表面化しやすい。",
  "BOM": "テキスト先頭に付く文字コードの目印。ACS の一部スクリプトは UTF-8 BOM 付きが前提。",
  "ホバー": "マウスを乗せること。乗せると説明が出る、の意。",
  "GPU": "画像処理に特化した演算装置。描画はここで行われる。",
  "API": "ライブラリの機能を外から使うための窓口(関数や型の取り決め)。例: ネットワークは <t>WinSock</t> をくるんだ薄い API として提供される。",
  "Bind": "<t>Observable</t> 同士を結びつけ、片方が変わると相手も自動で更新する MVVM の<t>データバインド</t>。双方向版は <code>TwoWayBind</code>。",
  "Clock": "起動からの経過時間を秒・ミリ秒・<t>ティック</t>で取れる高精度<t>タイマ</t>(すべて静的関数)。",
  "Connect": "<t>IBackendClient</t> で送信先(サーバや出力ファイル)を開いて通信を始めること。終了時は <code>Disconnect</code>。",
  "DLL": "実行時に読み込む共有ライブラリ(Windows の動的リンクライブラリ)。例: <t>ONNX Runtime</t> の本体を DLL として読み込む。",
  "Derived": "依存する <t>Observable</t> から値を計算し、依存が変わった時だけ再計算する派生値。<t>菱形依存</t>でも二重計算を避ける。",
  "EFormat": "<t>ピクセルフォーマット</t>(1 ピクセルのビット配置)を表す論理的な<t>enum</t>。DX12 / Vulkan で共通の名前で扱える。",
  "ETurnPhase": "ターン進行の<t>フェーズ</t>(プレイヤーの番/敵の番など)を表す<t>enum</t>。値はセーブ/リプレイに書ける安定値。",
  "Err": "<code>TResult</code> が失敗を表す側。<code>err.subcode</code> で原因を見分ける。成功側は <code>Ok</code>。",
  "Event": "ウィンドウや入力で起きた『出来事』1 つ分を表す構造体。種別に応じて中の<t>union</t>の対応フィールドだけが有効になる。",
  "FAcpakKey": "<t>acpak</t> の暗号化/復号に使う 256bit(32 バイト)の<t>鍵</t>。",
  "ANode": "<code>FTransform3D</code> と 2D helper を共有する統一シーンノード。<code>NewObject</code>/<code>TObjectPtr</code> で所有し、長期参照には <code>TWeakObjectPtr</code> を使う。",
  "FAnimationCurve": "キーを打って『時間 → 値』を補間する編集可能なカーブ。checked 更新・評価、Clamp/Loop/PingPong、33 種の easing preset に対応する。",
  "FAnimationCurveArchive": "<t>FAnimationCurve</t> の固定幅 little-endian 永続化 codec。version・exact size・CRC・全 key を検証し、復元は transactional に行う。",
  "FAnimationGraph": "3D スケルトンアニメ向けの<t>ステートマシン</t>型ブレンドグラフ。どの clip をどの比率で再生するかを制御する。",
  "FApplication": "メインループを回すアプリの基底クラス。継承して <code>OnStart</code>/<code>OnUpdate</code>/<code>OnRender</code>/<code>OnShutdown</code> を実装する。",
  "FBeatGrid": "リズムゲームの checked 譜面読み込みと timing 判定。通常 note に加え、<code>PressLane</code>/<code>ReleaseLane</code> で hold の head/tail を 1 回だけ集計する。",
  "FCurveKey": "<t>FAnimationCurve</t> の 1 key。有限な time/value/tangent と入口・出口の補間方式を保持する。",
  "FBuffDef": "バフ 1 種の定義(id・種別・持続秒・効果量・<t>スタック</t>方針など)。<code>RegisterBuff</code> で登録する。",
  "FCamera2D": "2D <t>カメラ</t>。位置/ズーム/回転、ターゲット追従、画面振動、world↔screen 座標変換を持つ。",
  "FCameraShakePresets": "『爆発/地震/着弾』などジャンル別の画面振動パラメータを名前付きで提供する preset ライブラリ。",
  "FCharacterCustomizer": "キャラの見た目(cosmetic)だけを slot 単位で着せ替えするマネージャ。戦闘性能はいじらない設計。",
  "FDebugDraw": "1 フレーム分のデバッグ線分(線/AABB/円/十字)を溜めるだけの<t>immediate-mode</t>バッファ。実描画は描画側が行う。",
  "FDialogueLocalizer": "会話の各行を翻訳キー扱いにして、現在の<t>ロケール</t>で翻訳を解決し<t>コールバック</t>へ渡す多言語化の橋渡し。",
  "FDialogueScript": "ノベル/アドベンチャー向けのシーンスクリプト再生機。セリフ・選択肢・ジャンプを命令列で宣言的に組む<t>ステートマシン</t>。",
  "FDungeonGenerator": "2D グリッドに部屋と廊下を配置する <t>BSP</t>(空間二分割)ダンジョン生成器。<t>シード</t>で結果を再現できる。",
  "FGame": "ゲームアプリの基底クラス。<t>FApplication</t> を継承し、シーン管理・固定タイムステップ・シーン遷移を内蔵する。",
  "FHotReloadEvent": "watcher が所有する UTF-8 path、monotonic timestamp、削除 flag を持つ 1 件の変更通知。",
  "FHotReloadWatcher": "Windows 開発ビルドの非同期 directory watcher と bounded event queue。再 import や MessageBroker publish は上位側で接続する。",
  "FKhronosOpenXrBridge": "<t>OpenXR</t> 公式ローダ(Khronos loader)を使った実<t>backend</t>。<t>HMD</t> や手の<t>ポーズ</t>を提供する。",
  "FMat4": "4x4 <t>行列</t>。平行移動・回転・拡大縮小、ビュー変換、投影変換を 1 つにまとめて表せる。3D 描画の心臓部。",
  "FMemorySystem": "メモリを目的・寿命別の<t>セグメント</t>に分け、各領域に独立予算を持たせる<t>メモリ</t>管理の中枢。",
  "FNodeId": "シーン内の <code>ANode</code> を指す index(24bit) + generation(8bit) の packed <t>ハンドル</t>。<t>stale</t> 検出に使う。",
  "FSceneClock": "シーン単位の論理時間を刻むクロック。pause・スロー・倍速を反映した <code>dt</code> を <t>Tween</t> やタイマーに供給する。",
  "FSequence": "「待つ→関数を呼ぶ→値を滑らかに変える」を 1 行で連鎖記述する演出ビルダー。作って <t>FSequenceRunner</t> に渡して再生する。",
  "FSequenceRunner": "複数の <t>FSequence</t> を同時に再生・管理する実行係。<code>Start</code> で所有して走らせ、毎フレーム <code>Tick</code> で進める。",
  "FSettings": "音量・解像度・キー設定などゲームをまたいで残したい値を型付き key-value で持つ小さなストア。INI 風テキストで保存/読込できる。",
  "FSourceLoc": "ソース上の位置(ファイル名・関数名・行・列)を持つ軽い型。<code>std::source_location</code> 相当で、引数の既定値に置くと呼び出し側の位置を自動取得できる。",
  "FWidget": "retained-mode UI ツリーの基底。子を <code>TUniquePtr</code> で所有し、layout・render・hit test・入力 hook を提供する。",
  "FStackPanel": "子<t>ウィジェット</t>を縦または横へ spacing/padding 付きで並べる layout panel。",
  "FContainer": "親の矩形を全ての子へ渡す、描画を持たない重ね合わせ用 panel。",
  "FLabel": "<code>TObservable&lt;FString&gt;</code> の text を表示する UI label。",
  "FButton": "click 完了を <code>TObservable&lt;bool&gt;</code> の pulse として公開する UI button。",
  "FSlider": "指定範囲の連続値を drag で編集し、<code>TObservable&lt;f32&gt;</code> で公開する UI slider。",
  "FCheckbox": "真偽値を click で反転し、label と observable state を持つ UI checkbox。",
  "FTextInput": "UTF-8 codepoint 境界を守る cursor 挿入・削除・移動と byte 上限を持つ 1 行入力欄。",
  "FUiRenderer": "<code>FWidget</code> ツリーを <code>FSpriteBatch</code>/<code>FFont</code> で描き、UTF-8 全体・prefix の幅も測る UI renderer。",
  "ASprite2DComponent": "<t>ANode</t> に付ける 2D スプライト描画コンポーネント。色付き矩形やテクスチャを 1 枚の四角い絵として表示する。",
  "ASpriteAnimComponent": "スプライトの UV 矩形を毎フレーム書き換えてスプライトシートアニメ(コマ送り)を再生するコンポーネント。",
  "FTileId": "タイル 1 セルの ID(<code>u16</code>)。<code>0</code> を「空(タイル無し)」として予約し、描画側が<t>アトラス</t>の index などとして解釈する。",
  "FTurnManager": "ターン制ゲームの進行を管理する部品。複数の陣営を行動順で並べ、各陣営が <t>AP</t>(行動ポイント)予算で行動する。",
  "FUiLayer": "<t>シーン</t>に数個のボタン/テキストを貼り付けるための薄い UI 層。シーンのライフサイクルに合わせて呼ぶだけで最小 UI が作れる。",
  "FVec2": "2 次元<t>ベクトル</t>(x, y)。2D の位置・速度・方向を表す基本型で、8 バイトと小さい。",
  "FVec3": "3 次元<t>ベクトル</t>(x, y, z)。3D の位置・方向・色などに使う最重要型で、<t>SIMD</t>演算に最適化されている。",
  "FVec4": "4 次元<t>ベクトル</t>(x, y, z, w)。RGBA 色や<t>同次座標</t>、<t>行列</t>との掛け算に使う。",
  "FViewModel": "<t>MVVM</t> のビューモデル(VM)の基底クラス。継承して <code>TObservable</code> なプロパティを並べ、画面に見せたい状態をまとめる。",
  "FWaveSpawner": "敵の<t>ウェーブ</t>(波状の出現)を管理する部品。複数の波をキューに並べ、全滅したら次の波へ進める<t>状態機械</t>を内蔵する。",
  "FWindow": "OS の<t>ウィンドウ</t>そのものを表すクラス。四角い領域を開き、閉じる要求や入力などの<t>イベント</t>を受け取る。ゲームの一番外側のループの中心。",
  "FWindowsCrashReporter": "Windows 専用のクラッシュ報告 backend。<t>DbgHelp</t> でミニダンプと人間が読めるテキスト報告書を書き出す。",
  "FrameTimer": "<t>フレーム</t>ごとの経過秒(<t>デルタタイム</t>)を計るタイマ。毎フレーム <code>Tick()</code> を呼ぶと前回からの経過秒を返す。",
  "GetDefaultWindowsCrashReporter": "プロセス全体で共有される既定の <t>FWindowsCrashReporter</t> <t>シングルトン</t>を返す関数。",
  "IAssetPackReader": "アセットパック(<code>.acpak</code> 等)からファイルを読み出す共通<t>インターフェース</t>。Mount してパス指定で中身を取り出す。",
  "IAudioBackend": "<code>FAudioDirector</code> から見た「実際に音を出す層」の純粋仮想<t>インターフェース</t>。Windows では XAudio2 実装が差し込まれる。",
  "IMatchmaker": "オンライン対戦の相手を探すマッチメイキングの<t>インターフェース</t>。検索開始→状態確認→受諾/取消の流れで使う。",
  "IOpenXrBridge": "XR ランタイムや <t>HMD</t> を抽象化した共通<t>インターフェース</t>。頭・手のポーズなどをこの型越しに叩き、具象 backend を差し替えて選ぶ。",
  "IShakeTarget": "画面ゆれ(shake)の trauma を受け取る側の純粋仮想<t>インターフェース</t>。<code>AddShake</code> など揺れ量の設定 API を持つ。",
  "Idle": "<t>状態機械</t>の「何もしていない/開始前」を表す既定状態。Idle→Walk のように、ここから他状態へ遷移する。",
  "Input": "キーボード・マウス・<t>ゲームパッド</t>の状態を問い合わせで読む静的クラス。毎フレーム先頭で <code>Update()</code> を呼んでから状態を読む。",
  "InstallWindowsCrashReporterAsDefault": "Windows のクラッシュ報告 backend を既定の provider に登録する結線ヘルパ。起動時に一度呼ぶと本物の backend が使われるようになる。",
  "Lerp": "2 つの値の間を比率 t(0〜1)でまっすぐ補間する関数(<t>線形補間</t>)。t=0 で始点、t=1 で終点。",
  "Network": "ネットワーク機能全体の初期化／終了をまとめる入口クラス(<t>WinSock</t> の <code>WSAStartup</code> ラッパ)。<t>ソケット</t>を使う前に一度 <code>Init()</code> を呼ぶ。",
  "OpenXrBridgeStub": "HMD が無くても落ちないよう「常に未初期化・原点ポーズ」を返す <t>IOpenXrBridge</t> の <t>スタブ</t>実装。実 backend 未リンクでも 2D 経路へ<t>フォールバック</t>できる。",
  "Present 可能状態": "描き終えた<t>バックバッファ</t>を画面に提示(Present)できるよう遷移させた状態。<code>EndRenderToSwapchain</code> 後にこの状態になる。",
  "ProjectileDef": "弾種ごとの挙動パラメータ(速度・寿命・重力・貫通・追尾など)をまとめた構造体。<code>RegisterDef</code> で『弾の設計図』として登録する。",
  "Reader": "<code>.acpak</code> などのアセットを<b>読む側</b>の部品。pak をマウントして仮想ファイルを取り出す(対になる書き出し側が Writer)。",
  "ScriptValue": "C++ とスクリプトの間で引数・戻り値を受け渡すための動的型付け値。Nil/Bool/Number/<t>FString</t>/Handle のタグ付き <t>POD</t>。",
  "ScriptVm": "スクリプト(Lua など)を読み込んで実行する仮想マシン。<code>IScriptVm</code> 越しに backend を差し替えられる。",
  "SparseSetBase": "<t>SparseSet</t> の型に依らない共通部分を提供する基底クラス。<code>World</code> がコンポーネント型を知らずに集合を扱うための内部基盤。",
  "SpawnRule": "ウェーブ内で「どの敵を・何体・どの間隔で・どこに」湧かせるかを定める 1 設定。<t>スポーンルール</t>。",
  "Storage": "設定やセーブデータ用の key-value ストア(INI 形式)。音量・解像度・進行度などを人間が読める形で永続化する。",
  "SystemFn": "ECS のシステム関数の型 <code>void(*)(World&amp;, f32 dt)</code>。<code>World</code> と<t>デルタタイム</t>を受け取り毎フレーム呼ばれる処理単位。",
  "SystemScheduler": "<t>SystemFn</t> を登録し、毎フレーム登録順にまとめて呼ぶ実行器。複数システムを一括で回すのに使う。",
  "TArray": "可変長配列(<code>std::vector</code> 相当)。末尾追加が速く要素は連続メモリに並ぶ。コピー禁止・<t>ムーブ</t>専用。",
  "TTF": "TrueType フォントのファイル形式。ACS は TTF/OTF を GPU <t>アトラス</t>に焼いて文字を描く。",
  "TUniquePtr": "1 人だけが所有する<t>スマートポインタ</t>(<code>std::unique_ptr</code> 相当)。コピー不可・<t>ムーブ</t>のみで、スコープを抜けると自動解放。",
  "TcpConnection": "確立済みの <t>TCP</t> 接続 1 本。順序どおり確実にデータを送受信でき、デストラクタで自動切断する。コピー不可・<t>ムーブ</t>のみ。",
  "TcpListener": "<t>TCP</t> 接続を待ち受ける側(サーバ)。<code>Accept()</code> でクライアントの接続を 1 本ずつ受理して <t>TcpConnection</t> にする。",
  "ViewModel": "<t>MVVM</t> の橋渡し層。実型 <code>FViewModel</code> と <code>TObservable</code> で状態を公開し、View(画面)がそれを<t>購読</t>して自動反映する。",
  "Widget": "UI 部品の総称。ACS の実型 <code>FWidget</code> は子を <t>TUniquePtr</t> で所有してツリーを作り、配置・描画・クリック判定・入力の仮想メソッドを持つ。",
  "f32": "32bit の浮動小数(<code>float</code> 相当)。座標・時間・係数など小数値に使う基本型。",
  "gameframework": "シーン・コンポーネント・各ジャンルキットなどゲーム作りの部品を集めた <code>acs::game</code> のモジュール。",
  "git diff": "Git の変更差分を表示するコマンド。テキスト保存形式はこれで読みやすいと版管理しやすい。",
  "tilemap": "タイルを格子状に敷き詰めて作る 2D マップ。<code>FTilemap</code> は複数レイヤのタイル ID 配列を持つ。",
  "u64": "64bit の符号なし整数(0 以上)。サイズ・<t>ハンドル</t>・大きな個数などに使う基本型。",
  "use-after-free": "すでに解放したメモリを誤って使ってしまうバグ。<t>世代</t>付き<t>ハンドル</t>などで検出・防止する。",
  "エスケープ": "<code>\"</code> や改行などの特殊文字を、壊れないよう安全な表現に置き換えること。",
  "キーフレーム": "タイムライン上の「この時刻にこれを起こす」を表す 1 点(時刻＋動作)。並べてカットシーンを組む。",
  "コピー": "中身をそっくり複製すること。ACS では高コストや所有権の事故を避けるため、多くの型でコピーを禁止し<t>ムーブ</t>のみ許す。",
  "コンテナ": "値をまとめて入れておく入れ物(<code>TArray</code> や <code>THashMap</code> など)。ACS は <t>STL</t> の代わりに独自のものを使う。",
  "シャドウパス": "影を作るために、シーンを光源から見た深度だけ先に描いておく描画工程。後の主<t>描画</t>で影の濃さに使う。",
  "シリアライザ": "オブジェクトの中身をファイルに保存できる形へ書き出し、また読み戻す部品。",
  "シーン": "タイトル・ゲーム本編・リザルトのような、画面の一場面とその進行をまとめた単位。",
  "ノード": "ゲーム世界に置く部品の単位。位置や<t>コンポーネント</t>を持ち、親子のツリーで並ぶ。",
  "ハッシュ": "文字列やデータを決まった大きさの数値へ変換すること。同じ入力からは常に同じ値が出る。",
  "パネル": "ImGui で作る、デバッグや編集用の小さな子ウィンドウ(調整画面)。",
  "フォント": "文字を画面に描くための字形データ。これを使って文字列を<t>描画</t>する。",
  "マクロ": "コンパイル前に決まった形へ展開される短い記述(<code>#define</code>)。定型コードを一行で書ける。",
  "マスク": "どの<t>レイヤ</t>に反応するかをビットで指定する絞り込み。立っているビットの相手だけが対象になる。",
  "ライフタイム": "オブジェクトが生まれてから壊れるまでの寿命。これを過ぎたものを触ると危険。",
  "レイヤ": "当たり判定や描画の対象を分類するための札。player/enemy/wall のように仲間分けする。",
  "レンダラ": "実際に画面へ絵を出す<t>描画</t>システム本体。",
  "乱数": "規則性のない数値。敵の出現位置やドロップ抽選などに使う。",
  "乱数生成器": "<t>乱数</t>を作り出す部品。同じ<t>シード</t>からは同じ並びを出せる。",
  "列挙(enum)": "決まった選択肢の集合を表す型。例: 上下左右のキー。E で始まる。",
  "単位行列": "掛けても相手を変えない行列(対角が 1)。何も変換しない状態を表す。",
  "同期": "処理が終わるまでその場で待つやり方。結果がすぐ手に入る代わりに止まる。",
  "循環依存": "モジュール A が B を、B が A を必要とし合い、まっすぐ組めなくなる状態。登録関数などで断ち切る。",
  "空間グリッド (SpatialGrid)": "空間を格子に区切り、近くにある物だけを速く探せるようにする仕組み。重なり検索に使う。",
  "結線": "部品同士をつなぎ込む(実装を既定の口に登録する)こと。これで上位が下位に直接依存せず使える。",
  "転置": "行列の行と列を入れ替える操作。",
  "逆行列": "ある変換を打ち消す行列。スクリーン→ワールド変換などに使う。",
  "選択肢": "会話などで提示する分岐の候補。プレイヤーが選ぶと進行が分かれる。",
  "非同期": "処理の完了を待たずに先へ進み、結果は後で受け取るやり方。重い読み込みなどで画面を止めない。",
  "高さフォグ": "高さが低いほど濃くなる指数フォグ(霧)。遠景や谷間の空気感を出す。",
  "FString": "ACS 標準の文字列型(<code>std::string</code> 相当)。短い文字列はヒープ無しで持つ最適化付き。",
  "Tween": "値を時間で滑らかに補間する仕組み。UI・カメラ・演出・シーン遷移に使う。",
  "acpak": "ACS のアセットアーカイブ形式(<code>.acpak</code>)。出荷時に複数ファイルを 1 つにまとめる。",
  "カメラ": "視点。world↔screen の座標変換(ビュー/投影)を作る。2D は <code>FCamera2D</code>。",
  "データバインド": "<code>TObservable</code> な状態と UI を結びつけ、変更を自動で反映させる MVVM の仕組み。",
  "バックバッファ": "描画先の裏画面。1 フレーム描き終えたら表に提示(present)して画面を更新する。",
  "メモリ": "プログラムが使う記憶領域。確保と解放の管理が性能と安全性を左右する。"
});
