<UserControl x:Class="AcsEditor.AssetBrowserPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Panel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ツールバー: 上へ / 現在パス / 検索 / 更新 / インポート -->
        <DockPanel Grid.Row="0" Margin="6,5" LastChildFill="True">
            <Button x:Name="UpBtn" DockPanel.Dock="Left" Content="↑" Width="30"
                    ToolTip="親フォルダへ" Click="OnUp"/>
            <Button DockPanel.Dock="Right" Content="インポート…" Margin="6,0,0,0" Click="OnImport"/>
            <Button DockPanel.Dock="Right" Content="更新" Click="OnRefresh"/>
            <TextBox x:Name="SearchBox" DockPanel.Dock="Right" Width="160" Margin="6,0,0,0"
                     VerticalContentAlignment="Center" ToolTip="名前で検索" TextChanged="OnSearchChanged"/>
            <TextBlock x:Name="PathText" VerticalAlignment="Center" Margin="10,0"
                       Foreground="{DynamicResource TextDim}" TextTrimming="CharacterEllipsis"/>
        </DockPanel>

        <!-- 左: タイル一覧 / 右: プレビュー + 情報 (参考エンジン風) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="296"/>
            </Grid.ColumnDefinitions>

        <ListBox x:Name="Tiles" Grid.Column="0" Background="{DynamicResource Panel2}" BorderThickness="0"
                 Margin="4,0,4,4" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 MouseDoubleClick="OnTileDoubleClick" PreviewMouseMove="OnTilePreviewMouseMove"
                 SelectionChanged="OnTileSelected"
                 PreviewMouseLeftButtonDown="OnTileMouseDown" PreviewMouseRightButtonDown="OnTileRightDown">
            <ListBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="シーンに配置 (Place)" Click="OnCtxPlace"/>
                    <MenuItem Header="開く (Open)" Click="OnCtxOpen"/>
                    <Separator/>
                    <MenuItem Header="Blueprint に変換 (旧 Prefab→新)" Click="OnCtxConvert"/>
                </ContextMenu>
            </ListBox.ContextMenu>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="3"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd" Background="Transparent" CornerRadius="0" Padding="4">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource CtrlHover}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#883C9AE8"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="78">
                        <!-- 画像はサムネイル、それ以外は種別グリフ (背後の Border) -->
                        <Grid Width="64" Height="64" HorizontalAlignment="Center">
                            <Border CornerRadius="0" Background="{Binding GlyphBrush}">
                                <TextBlock Text="{Binding Glyph}" Foreground="White" FontWeight="SemiBold"
                                           FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <Image Source="{Binding Thumb}" Stretch="Uniform" SnapsToDevicePixels="True"/>
                        </Grid>
                        <TextBlock Text="{Binding Name}" Foreground="{DynamicResource Text}" FontSize="11"
                                   TextAlignment="Center" TextTrimming="CharacterEllipsis"
                                   Margin="0,4,0,0" MaxWidth="74" ToolTip="{Binding Name}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch"
                      Background="{DynamicResource Hairline}"/>

        <!-- プレビュー + 情報ペイン -->
        <Grid Grid.Column="2" Background="{DynamicResource Panel}" Margin="0,0,2,4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="PREVIEW" Style="{DynamicResource Section}" Margin="10,7,0,4"/>
            <Grid Grid.Row="1" Margin="10,0">
                <Border x:Name="PreviewGlyphBox" Background="{DynamicResource Ctrl}" CornerRadius="2">
                    <TextBlock x:Name="PreviewGlyph" Text="" Foreground="{DynamicResource TextDim}"
                               FontSize="26" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Image x:Name="PreviewImage" Stretch="Uniform" SnapsToDevicePixels="True"
                       RenderOptions.BitmapScalingMode="HighQuality" Visibility="Collapsed"/>
            </Grid>
            <StackPanel Grid.Row="2" Margin="10,8,10,8">
                <TextBlock x:Name="PreviewNameText" Foreground="{DynamicResource Text}" FontWeight="SemiBold"
                           FontSize="12.5" TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="PreviewMetaText" Foreground="{DynamicResource TextDim}" FontSize="11"
                           TextWrapping="Wrap" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>
        </Grid>
    </Grid>
</UserControl>
