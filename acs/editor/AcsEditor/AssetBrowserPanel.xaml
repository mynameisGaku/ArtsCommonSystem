<UserControl x:Class="AcsEditor.AssetBrowserPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource Panel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ツールバー: 上へ / 現在パス / 更新 / インポート -->
        <DockPanel Grid.Row="0" Margin="6,5" LastChildFill="True">
            <Button x:Name="UpBtn" DockPanel.Dock="Left" Content="↑" Width="30"
                    ToolTip="親フォルダへ" Click="OnUp"/>
            <Button DockPanel.Dock="Right" Content="インポート…" Margin="6,0,0,0" Click="OnImport"/>
            <Button DockPanel.Dock="Right" Content="更新" Click="OnRefresh"/>
            <TextBlock x:Name="PathText" VerticalAlignment="Center" Margin="10,0"
                       Foreground="{DynamicResource TextDim}" TextTrimming="CharacterEllipsis"/>
        </DockPanel>

        <!-- タイル一覧 -->
        <ListBox x:Name="Tiles" Grid.Row="1" Background="{DynamicResource Panel2}" BorderThickness="0"
                 Margin="4,0,4,4" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 MouseDoubleClick="OnTileDoubleClick" PreviewMouseMove="OnTilePreviewMouseMove"
                 PreviewMouseLeftButtonDown="OnTileMouseDown">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="3"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd" Background="Transparent" CornerRadius="4" Padding="4">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="{DynamicResource CtrlHover}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#883C9AE8"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="78">
                        <!-- 画像はサムネイル、それ以外は種別グリフ (背後の Border) -->
                        <Grid Width="64" Height="64" HorizontalAlignment="Center">
                            <Border CornerRadius="4" Background="{Binding GlyphBrush}">
                                <TextBlock Text="{Binding Glyph}" Foreground="White" FontWeight="SemiBold"
                                           FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <Image Source="{Binding Thumb}" Stretch="Uniform" SnapsToDevicePixels="True"/>
                        </Grid>
                        <TextBlock Text="{Binding Name}" Foreground="{DynamicResource Text}" FontSize="11"
                                   TextAlignment="Center" TextTrimming="CharacterEllipsis"
                                   Margin="0,4,0,0" MaxWidth="74" ToolTip="{Binding Name}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
