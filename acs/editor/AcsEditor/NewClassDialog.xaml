<Window x:Class="AcsEditor.NewClassDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="新規クラス — 基底クラスを選択" Height="420" Width="460"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource Win}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="クラス名" Foreground="{DynamicResource TextDim}" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" x:Name="NameBox" Text="FMyComponent" Margin="0,0,0,14"/>

        <TextBlock Grid.Row="2" Text="基底クラス (継承元) — 階層から選択。基底の基底も入れ子で表示。"
                   Foreground="{DynamicResource TextDim}" Margin="0,0,0,4"/>
        <TreeView Grid.Row="3" x:Name="BaseTree" Height="170" Margin="0,0,0,10"
                  Background="#FF181C23" Foreground="#FFD8DEE9" BorderBrush="#FF3A4452"/>

        <TextBlock Grid.Row="4" x:Name="HintText" TextWrapping="Wrap" VerticalAlignment="Top"
                   Foreground="{DynamicResource TextDim}" FontSize="11"/>

        <DockPanel Grid.Row="5" LastChildFill="False">
            <CheckBox x:Name="BuildAfterBox" DockPanel.Dock="Left" IsChecked="True"
                      VerticalAlignment="Center" Foreground="{DynamicResource Text}"
                      Content="生成後にビルドして反映"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="キャンセル" Width="96" Height="32" Margin="0,0,8,0" Click="OnCancel"/>
                <Button Content="生成" Width="96" Height="32" Click="OnCreate" FontWeight="SemiBold"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
