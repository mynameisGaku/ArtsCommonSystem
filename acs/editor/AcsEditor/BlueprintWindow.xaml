<Window x:Class="AcsEditor.BlueprintWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AcsEditor"
        Title="Blueprint" Height="660" Width="1080"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource Win}"
        Foreground="{DynamicResource Text}" UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display" FontSize="12.5">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="236"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ブラックボード: 宣言済みの名前付き変数 -->
        <DockPanel Grid.Column="0" Background="{DynamicResource Panel}">
            <TextBlock DockPanel.Dock="Top" Text="Blackboard" Style="{DynamicResource PanelHeader}"/>
            <Border DockPanel.Dock="Top" Height="1" Background="{DynamicResource Hairline}"/>
            <TextBlock DockPanel.Dock="Top" TextWrapping="Wrap" Foreground="{DynamicResource TextDim}" FontSize="10.5"
                       Margin="11,9,11,9" LineHeight="15"
                       Text="名前付き変数を宣言します。実行開始時に変数へ種まきされ、実行後は現在値を緑で表示。Get / Set Variable ノードから参照できます。"/>

            <!-- 追加フォーム (下部に固定) -->
            <Border DockPanel.Dock="Bottom" Background="{DynamicResource Panel2}" BorderBrush="{DynamicResource Hairline}"
                    BorderThickness="0,1,0,0" Padding="9,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="BbName"  Grid.Column="0" Margin="0,0,5,0" ToolTip="キー名"/>
                    <TextBox x:Name="BbValue" Grid.Column="1" Margin="0,0,5,0" ToolTip="既定値"/>
                    <Button  Grid.Column="2" Content="＋ 追加" Padding="9,3" Click="OnAddKey" Foreground="{DynamicResource OkFg}"/>
                </Grid>
            </Border>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="BbList" Margin="9,6,9,6"/>
            </ScrollViewer>
        </DockPanel>

        <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="{DynamicResource Edge}"/>

        <local:BlueprintEditor x:Name="GraphHost" Grid.Column="2"/>
    </Grid>
</Window>
